@* @model Product

<div class="card mb-2">
    <img class="card-img-top" src="~/img/@Model.ImageUrl" alt="">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <small class="text-muted">
            <i class="fas fa-lira-sign"></i>@Model.Price
        </small>
    </div>
    <div class="card-footer text-center">
        <a asp-controller="shop" asp-action="Details" asp-route-url="@Model.Url" class="btn btn-primary btn-sm">Details</a>

        @if(User.Identity.IsAuthenticated)
        {
            <form asp-controller="Cart" asp-action="AddToCart" method="POST" style="display: inline;">
                <input type="hidden" name="productId" value="@Model.ProductId">
                <input type="hidden" name="quantity" value="1">
                <button type="submit" class="btn btn-primary btn-sm">Add to Cart</button>
            </form>
        }
    </div>
</div>    *@


@* @model Product

<div class="card mb-4 shadow-sm">
    <img class="card-img-top" src="~/img/@Model.ImageUrl" alt="@Model.Name" style="height: 200px; object-fit: cover;">
    <div class="card-body">
        <h5 class="card-title text-truncate">@Model.Name</h5>
        <p class="card-text text-muted">
            <i class="fas fa-lira-sign"></i> @Model.Price
        </p>
        <div class="d-flex justify-content-between align-items-center">
            <a asp-controller="shop" asp-action="Details" asp-route-url="@Model.Url" class="btn btn-primary btn-sm">Details</a>

            @if(User.Identity.IsAuthenticated)
            {
                <form asp-controller="Cart" asp-action="AddToCart" method="POST" class="m-0 p-0">
                    <input type="hidden" name="productId" value="@Model.ProductId">
                    <input type="hidden" name="quantity" value="1">
                    <button type="submit" class="btn btn-outline-primary btn-sm">Add to Cart</button>
                </form>
            }
        </div>
    </div>
</div>

<style>
    .card {
        border: none;
        border-radius: 10px;
        transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }

    .card-img-top {
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
    }

    .card-title {
        font-size: 1.1rem;
        font-weight: 600;
    }

    .card-text {
        font-size: 1rem;
    }

    .btn-primary, .btn-outline-primary {
        font-size: 0.875rem;
        padding: 0.5rem 1rem;
    }

    @@media (max-width: 576px) {
        .card-title {
            font-size: 1rem;
        }

        .btn-primary, .btn-outline-primary {
            font-size: 0.75rem;
            padding: 0.4rem 0.8rem;
        }
    }
</style> *@

@model Product

<div class="card mb-4 mt-4 shadow-sm">
  
<div class="card" style="width: 100%; height: 200px; overflow: hidden;">
    <img class="card-img-top" src="~/img/@Model.ImageUrl" alt="@Model.Name" style="width: 100%; height: 100%; object-fit: contain;">
</div>


    <div class="card-body">
        <h5 class="card-title text-truncate">@Model.Name</h5>
        <p class="card-text text-muted">
            <i class="fas fa-lira-sign"></i> @Model.Price
        </p>
        <div class="d-flex justify-content-between align-items-center">
            <a asp-controller="shop" asp-action="Details" asp-route-url="@Model.Url" class="btn btn-outline-secondary btn-sm">Details</a>

            @if(User.Identity.IsAuthenticated)
            {
                <form asp-controller="Cart" asp-action="AddToCart" method="POST" class="m-0 p-0">
                    <input type="hidden" name="productId" value="@Model.ProductId">
                    <input type="hidden" name="quantity" value="1">
                    <button type="submit" class="btn btn-secondary btn-sm">Add to Cart</button>
                </form>
            }
        </div>
    </div>
</div>

@* CSS Kodu *@

<style>
    .card {
        border: none;
        border-radius: 10px;
        transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }

    .card-img-top {
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
    }

    .card-title {
        font-size: 1.1rem;
        font-weight: 600;
    }

    .card-text {
        font-size: 1rem;
    }

    .btn-secondary, .btn-outline-secondary {
        font-size: 0.875rem;
        padding: 0.5rem 1rem;
    }

    @@media (max-width: 576px) {
        .card-title {
            font-size: 1rem;
        }

        .btn-secondary, .btn-outline-secondary {
            font-size: 0.75rem;
            padding: 0.4rem 0.8rem;
        }
    }
</style>




